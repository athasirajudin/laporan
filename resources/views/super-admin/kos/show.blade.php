@extends('layouts.app')
@section('title','Detail Kos')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1">{{ $kos->nama_kos }}</h1><p class="text-secondary mb-0">Detail kos, pemilik, wilayah, dan penghuni.</p></div><div class="d-flex gap-2">@if($kos->status === 'pending')<form method="POST" action="{{ route('super-admin.kos.verify',$kos) }}">@csrf @method('PATCH')<button class="btn btn-success">Setujui</button></form><form method="POST" action="{{ route('super-admin.kos.reject',$kos) }}">@csrf @method('PATCH')<button class="btn btn-outline-danger">Tolak</button></form>@endif</div></div>
<div class="row g-4 mb-4"><div class="col-lg-8"><div class="card border-0 shadow-sm"><div class="card-body"><dl class="row mb-0"><dt class="col-sm-3">Pemilik</dt><dd class="col-sm-9">{{ $kos->user->name }}</dd><dt class="col-sm-3">Wilayah</dt><dd class="col-sm-9">RT {{ $kos->wilayah->rt }}/RW {{ $kos->wilayah->rw }} — {{ $kos->wilayah->kelurahan }}</dd><dt class="col-sm-3">Alamat</dt><dd class="col-sm-9">{{ $kos->alamat }}</dd><dt class="col-sm-3">Status</dt><dd class="col-sm-9">{{ ucfirst($kos->status) }}</dd></dl></div></div></div></div>
<div class="card border-0 shadow-sm"><div class="card-header bg-white"><h2 class="h5 mb-0">Penghuni</h2></div><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th class="ps-3">Nama</th><th>Pekerjaan</th><th>Masuk</th><th>Keluar</th><th>Status</th></tr></thead><tbody>@forelse($kos->penghuni as $penghuni)<tr><td class="ps-3">{{ $penghuni->nama_lengkap }}</td><td>{{ $penghuni->pekerjaan }}</td><td>{{ $penghuni->tanggal_masuk?->format('d-m-Y') }}</td><td>{{ $penghuni->tanggal_keluar?->format('d-m-Y') ?? '-' }}</td><td>{{ $penghuni->status === 'active' ? 'Aktif' : 'Sudah Keluar' }}</td></tr>@empty<tr><td colspan="5" class="text-center py-4 text-secondary">Belum ada penghuni.</td></tr>@endforelse</tbody></table></div></div>
@endsection
